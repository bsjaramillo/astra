[package]
name = "astra-admission"
description = "Admission compartida entre los transports TCP nativo y web: un único orden de gates"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
server-core = { path = "../server-core" }
astra-scripting = { path = "../scripting" }
tracing = { workspace = true }
rand = { workspace = true }
